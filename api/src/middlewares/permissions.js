// import { permissions as userPermissions } from '../schema/user';
import { permissions as formPermissions } from '../schema/form';
import { permissions as logoPermissions } from '../schema/logo';

export const permissions = [formPermissions, logoPermissions];

export default {
  permissions
};
